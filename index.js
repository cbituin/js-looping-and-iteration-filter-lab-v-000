// Code your solution in this file
const findMatching = (arr, name) => {
  let matchingData = arr.push(arr.filter(val => val === name));
};

const fuzzyMatch = () => {
  
};

const matchName = () => {
  
};