// Code your solution in this file
const findMatching = (arr, name) => {
  let matchingData = [];
  arr.filter(val => val === name);
};

const fuzzyMatch = () => {
  
};

const matchName = () => {
  
};