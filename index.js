// Code your solution in this file
const findMatching = (arr, name) => {
  let matchingData = [];
  matchingData.push(arr.filter(val => val === name));
  return matchingData;
};


const fuzzyMatch = () => {
  
};

const matchName = () => {
  
};